import { usePageTitle } from "@/hooks";

export const SignIn = () => {
  usePageTitle("Sign In");

  return (
    <section>
      <header>
        <h1>Sign In</h1>
      </header>
    </section>
  );
};
