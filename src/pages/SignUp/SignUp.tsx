import { usePageTitle } from "@/hooks";

export const SignUp = () => {
  usePageTitle("Sign Up");

  return (
    <section>
      <header>
        <h1>Sign Up</h1>
      </header>
    </section>
  );
};
